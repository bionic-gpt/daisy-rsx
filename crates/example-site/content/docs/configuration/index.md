# Configuration

Use this page to verify docs sidebar highlighting across multiple pages.

## Site settings

The example site keeps its navigation, metadata, and assets in Rust modules so generated pages can share the same layout.

## Local build

Run the example site from its crate directory when generating static output.
